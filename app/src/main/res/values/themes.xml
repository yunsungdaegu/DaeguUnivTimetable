<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.DaeguUnivTimetable" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="PowerSpinnerCustom">
        <item name="android:gravity">center</item>
        <item name="android:padding">10dp</item>
        <item name="android:textSize">15sp</item>
        <item name="android:textColorHint">@color/black</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:background">@android:color/holo_green_light</item>
        <item name="spinner_divider_show">true</item>
        <item name="spinner_item_height">46dp</item>
        <item name="spinner_popup_animation">dropdown</item>
    </style>

    <style name="Theme.DaeguUnivTimetable" parent="Base.Theme.DaeguUnivTimetable" />
</resources>