<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <import type="kr.ac.daegu.timetable.presentation.utils.Constants"/>
        <import type="android.view.View"/>
        <variable
            name="vm"
            type="kr.ac.daegu.timetable.presentation.SemesterViewModel" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".presentation.SemesterActivity">
        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingStart="30dp"
            android:paddingEnd="30dp">
            <TextView
                android:id="@+id/title"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toTopOf="@id/next"
                android:text="@string/semester_title"
                android:textSize="25sp"
                android:gravity="center"
                app:layout_constraintVertical_bias="0.35"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"/>
            <androidx.constraintlayout.widget.ConstraintLayout
                android:id="@+id/semester_layout"
                android:orientation="horizontal"
                app:layout_constraintTop_toBottomOf="@id/title"
                android:layout_marginTop="30dp"
                android:layout_width="match_parent"
                android:layout_height="wrap_content">
                <com.skydoves.powerspinner.PowerSpinnerView
                    style="@style/PowerSpinnerCustom"
                    android:id="@+id/semester_year"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:hint="@string/semester_year"
                    android:layout_marginEnd="10dp"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintEnd_toStartOf="@id/semester"/>
                <com.skydoves.powerspinner.PowerSpinnerView
                    style="@style/PowerSpinnerCustom"
                    android:id="@+id/semester"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:hint="@string/semester"
                    android:layout_marginStart="10dp"
                    app:spinner_item_array="@array/semester"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintStart_toEndOf="@id/semester_year"
                    app:layout_constraintEnd_toEndOf="parent"/>
            </androidx.constraintlayout.widget.ConstraintLayout>
            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/next"
                android:text="@string/semester_next"
                app:layout_constraintBottom_toBottomOf="parent"
                android:layout_marginBottom="50dp"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"/>
        </androidx.constraintlayout.widget.ConstraintLayout>
        <View
            android:clickable="true"
            android:focusable="true"
            android:background="@color/shadow"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="@{vm.isFlow(Constants.READY) ? View.VISIBLE : View.GONE}"/>
        <ProgressBar
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:visibility="@{vm.isFlow(Constants.READY) ? View.VISIBLE : View.GONE}"
            android:layout_width="70dp"
            android:layout_height="70dp"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>